<!-- have these call something that allows them display all the info, edit them, delete them --->
<div class="d-flex flex-row" > 
  <% case project_entry.status %>
    <% when "assigned" %>
        <%= button_to project_entry_path(project_entry), method: :patch, params: { project_entry: { status: "new" , assigned_id:  nil, assigned_fullname: nil} } do %>
        <
        <% end %>
    <% when "accepted" %>
        <%= button_to project_entry_path(project_entry), method: :patch, params: { project_entry: { status: "assigned" } } do %>
        <
        <% end %>
    <% when "in_progress" %>
        <%= button_to  project_entry_path(project_entry), method: :patch, params: { project_entry: { status: "accepted" } } do %>
        <
        <% end %>
    <% when "completed" %>
        <%= button_to project_entry_path(project_entry), method: :patch, params: { project_entry: { status: "in_progress" } } do %>
        <
        <% end %>
    <% else %>
        <button disabled><</button>
  <% end %>
      <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target=<%="#popup-project_entry"+ project_entry.id.to_s%>>
    
    <div class="d-flex flex-row">
      <h2>
          <%= project_entry.title %>
      </h2>
      <h2 class="ms-auto">
          <%= project_entry.priority %>
      </h2>
    </div>
    <p>
      <%= project_entry.description %>
    </p> 
    </button>

  <% case project_entry.status %>
    <% when "new" %>
      <%= button_to project_entry_path(project_entry), method: :patch, params: { project_entry: { status: "assigned", assigned_id:  @current_id, assigned_fullname: @current_fullname} } do %>
        >
      <% end %>
    <% when "assigned" %>
      <%= button_to  project_entry_path(project_entry), method: :patch, params: { project_entry: {  status: "accepted" } } do %>
      >
      <% end %>    
    <% when "accepted" %>
      <%= button_to  project_entry_path(project_entry), method: :patch, params: { project_entry: {  status: "in_progress" } } do%>
      >
      <% end %>    
    <% when "in_progress" %>
      <%= button_to  project_entry_path(project_entry), method: :patch, params: { project_entry: {  status: "completed" } } do%>
      >
      <% end %>
    <% else %>
  <button disabled>></button>
  <% end %>
</div>
<div class="modal" id=<%="popup-project_entry"+ project_entry.id.to_s%> tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel">Project Entry</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
         <%= turbo_frame_tag "project_entry", src: project_entry_path(project_entry), target: "_top"%>
      </div>
    </div>
  </div>
</div>